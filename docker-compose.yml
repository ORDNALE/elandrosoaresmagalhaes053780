services:

  postgres:
    image: postgres:16
    container_name: app-postgres
    environment:
      POSTGRES_DB: musicdb
      POSTGRES_USER: appuser
      POSTGRES_PASSWORD: app123
    ports:
      - "5444:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U appuser -d musicdb"]
      interval: 5s
      timeout: 5s
      retries: 5

  minio:
    image: minio/minio:latest
    container_name: app-minio
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
      MINIO_SERVER_URL: http://localhost:19000
    ports:
      - "19000:9000"
      - "19001:9001"
    volumes:
      - miniodata:/data
    healthcheck:
      test: ["CMD", "mc", "ready", "local"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: ./backend
    container_name: elandro-api
    network_mode: host
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_started
    environment:
      QUARKUS_HTTP_PORT: 8090
      QUARKUS_DATASOURCE_JDBC_URL: jdbc:postgresql://localhost:5444/musicdb
      QUARKUS_DATASOURCE_USERNAME: appuser
      QUARKUS_DATASOURCE_PASSWORD: app123
      QUARKUS_MINIO_HOST: localhost
      QUARKUS_MINIO_PORT: 19000
      QUARKUS_MINIO_SECURE: "false"
      QUARKUS_MINIO_ACCESS_KEY: minioadmin
      QUARKUS_MINIO_SECRET_KEY: minioadmin
      MINIO_BUCKET_NAME: capa-albuns

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: elandro-frontend
    network_mode: host
    restart: unless-stopped
    depends_on:
      api:
        condition: service_started

volumes:
  pgdata:
  miniodata:
